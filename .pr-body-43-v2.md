## 背景

Issue #43（Parent #34）：保守档只有四种 Go 固定模板，高权限宿主命令是 CLI-only。本 PR 交付工作区范围、一次性、无持久 Shell 的通用命令 Runner 的 Go 核心 + Tool Gateway 提案链 + CLI + 四档权限门 + 审计。

## 本次改动

- **once_command.v1 结构化协议**（internal/runner/once_command.go）：executable + 字面量 argv + cwd + env + timeout + purpose，协议中不存在 Shell 字符串；Shell 解释器（cmd/powershell/pwsh/bash/sh/zsh/fish/wscript/cscript…）整体拒绝，Windows 只允许 Workspace 外的 .exe/.com 原生二进制（.bat/.cmd 包装被拒）；cwd 经 EvalSymlinks 强制 Workspace 内（拒绝 ..、symlink/junction/reparse 逃逸）；env 仅 SystemRoot/WINDIR/TEMP/TMP allowlist，不继承 Agent 环境、不加载任何 Profile。
- **进程树级执行器**：Windows PROC_THREAD_ATTRIBUTE_JOB_LIST 创建时绑定 kill-on-close Job Object（超时/取消终止完整进程树）；Unix 独立进程组 + SIGKILL。输出 ≤64KiB + UTF-8 修复 + Secret 脱敏 + 前缀哈希，只返回不可信证据（exit code/duration/计数/termination 标志）。
- **Tool Gateway 提案链**：新工具 one_shot_command_propose（ClassAgentProposal、fenced root Supervisor scope）。schema v105 once_command_proposals：不可变提案（env 值永不落库，只存排序 key 集合+摘要）；操作键幂等；approve 绑定 approval_fingerprint，审阅后参数不可变；proposed→approved/denied→executed 状态机受 CHECK 约束。提案阶段即用真实 Workspace root 做完整边界校验。
- **四档权限门 + 审计**：经 executionauth（Kind=stateless_command）逐档裁决——conservative 拒绝（提案可记录、approve 拒绝）、approval 逐条批准（once-command review approve）、full_access 一次性执行+审计（--enable-danger-full-access）、debug 不建持久 Shell；执行写 run 事件 once_command.executed（metadata-only，不落 raw output/env/argv）。
- **指纹链**：Spec → Request（绑定 Run/Workspace）→ Approval（绑定审批身份）。
- **CLI**：once-command run（直接执行）、once-command proposals|review approve|deny|run --proposal（审阅链路）。

## 测试覆盖

- runner 层：验证矩阵（Shell 解释器/Workspace 逃逸/Workspace 内可执行文件/env 越权/坏 UTF-8/超时边界）、symlink 目录逃逸、指纹绑定、Windows 真实原生二进制执行（Job Object 路径）、Unix echo 与超时进程终止（-race）。
- application 层（真实 store + 真实权限快照）：四档行为矩阵、审计事件落库、Workspace 逃逸与 powershell.exe 拒绝；提案→审阅→执行全链路（幂等重放、环境 drift 拒绝、approval 指纹绑定、二次审阅冲突、executed 状态）、conservative approve 拒绝、提案阶段 Shell/逃逸拒绝。

## 验证结果

- go build ./...、go vet ./...、go test -timeout 30m ./... 全绿（SUITE=0，两轮）；runner once 路径 -race 通过。

## 安全边界与非目标

- 完全访问只减少逐条审批，不让模型修改启动闸门/Workspace Scope/审计/永久删除守卫；重启后按进程级 gate 重新裁决（Allows 语义），无持久授权。
- 不实现持久 ConPTY；不支持隐式 &&/;/管道/重定向/命令替换；不负责 Docker Sandbox 与 Git 专用语义。
- 后续 PR（ADR 0107 已记录）：Desktop 控制面与 HTTP/OpenAPI。

Closes #43
